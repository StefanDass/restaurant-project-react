import React from 'react';

const RecommendationDetails = ({ name }) => {
    
    return (
        <>
            <h5>We want: {name}</h5>
        </>
    );
};

export default RecommendationDetails;